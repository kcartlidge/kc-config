{
  "name": "kc-config",
  "version": "1.0.0",
  "description": "Very simple, fast and easy to use config loader. Gets from file(s), overridden by environment.",
  "main": "./lib/index.js",
  "scripts": {
    "start": "node ./example/index.js",
    "test": "mocha ./test"
  },
  "keywords": [
    "config",
    "file",
    "environment"
  ],
  "author": "K Cartlidge",
  "license": "MIT",
  "devDependencies": {
    "expect": "^1.20.2",
    "mocha": "^3.2.0",
    "sinon": "^2.0.0"
  },
  "dependencies": {
    "bluebird": "^3.5.0"
  }
}
